name: example
description: dart_tefip usage example for terminal integration
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  dart_tefip:
    path: ../

dev_dependencies:
  lints: ^6.0.0
