name: example
description: dart_tefip usage example for terminal integration
version: 1.1.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_tefip:
    path: ../

dev_dependencies:
  lints: ^6.0.0
